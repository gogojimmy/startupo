head.admin_category
  h1= I18n.t('admin_category.new')
= simple_form_for @category, :url => admin_admin_categories_path do |f|
  = render :partial => 'form', :locals => {:f => f}
= link_to I18n.t('admin_category.back_to_index'), admin_admin_categories_path,
                                                 :class => 'btn'
